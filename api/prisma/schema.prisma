// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://aws_movie_app_user:test1234@staging-db.cdoobi4js1mb.eu-west-1.rds.amazonaws.com:5432/aws_movie_app_db"
}

model Movie {
  id        String   @id @default(uuid())
  title     String
  createdAt DateTime @default(now())
  rating    Float
}
